import React from "react";
import ReactDOM from "react-dom";
import App, { Appp } from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
